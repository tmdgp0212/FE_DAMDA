import React from 'react';
import FAQList from '../FAQList';

function FrequentlyAskedQuestions({ enteredFAQs }) {
  return <FAQList faqList={enteredFAQs} />;
}

export default FrequentlyAskedQuestions;
